# File Index — Electron app

> Part of [pi-agent-dashboard file index](./file-index.md). Loaded on demand.
>
> **Change-history annotations** (e.g. *"See change: foo-bar"*) → OpenSpec changes archived under `openspec/changes/archive/`.
>
> **Update protocol**: see `AGENTS.md` → "Documentation Update Protocol".

| File | Purpose |
|------|---------|
| `packages/electron/src/__tests__/clean-install-smoke.test.ts` | Cross-failure regression checkpoints (5 tests): server/installer log paths distinct, scope-dir wipe recoverable, managed-dir-root resolves, isDashboardRunning honors opts, watchdog routes crash unless graceful. See change: streamline-electron-bootstrap-and-recovery. |
| `packages/electron/src/__tests__/loading-page-log-path.test.ts` | Regression: `readServerLogTail` reads `~/.pi/dashboard/server.log`, NOT `~/.pi-dashboard/server.log`. Failure 3. See change: streamline-electron-bootstrap-and-recovery. |
| `packages/electron/src/__tests__/server-watchdog-respawn.test.ts` | Tests `makeServerWatchdog` factory + `gracefulShutdownInProgress` flag lifecycle. Failure 5. See change: streamline-electron-bootstrap-and-recovery. |
| `packages/electron/src/lib/health-check.ts` | Thin re-export of `isDashboardRunning` from shared `server-identity.js`. Single source of truth post-Failure-4. See change: streamline-electron-bootstrap-and-recovery. |
| `packages/electron/src/main.ts` | Electron main process: single-instance, wizard, server launch, loading page, tray. **Group-16 Failures 4+5** (change: streamline-electron-bootstrap-and-recovery): pre-wizard probe uses retry opts `{timeoutMs:8000, retries:3, retryDelayMs:500}`; log line widened to `running=... portConflict=... pid=...`. `before-quit` listener flips `setGracefulShutdownInProgress(true)`. Watchdog wired to `showLoadingPage` on unexpected child crash. **Preflight reconciliation** (change: streamline-electron-bootstrap-and-recovery): on every launch, before server health check, `preflightAndPromptForReinstall(...)` runs `runPreflight({managedDir, resourcesPath, buildTimePinsPath})`; on `needsAction` shows dialog (Reinstall now / Skip / Open Doctor) or silent auto-reinstall via `SILENT_PREFLIGHT=1`; passes `diff.upToDate` as `installStandalone` `skipPackages` so current packages skipped. Wired into BOTH LaunchSource V2 + legacy branches. Registers recovery IPC channels (`piDashboard.preflightCheck` / `reinstall` / `forceReinstall`) consumed by loading.html + doctor.html. **Power-user-mode managed install** (change: fix-electron-windows-installer-and-server-bootstrap, Defect 1): firstRun `pi.found && bridge.found` auto-skip no longer skips `installStandalone()`. Auto-skip removes wizard UI; managed install (tsx + pi@0.70.0 + openspec from offline cacache) runs so bundled server's runtime can resolve TS loader from `~/.pi-dashboard/`. Decision logic in pure `decideStartupAction(state)` helper (`lib/power-user-install.ts`), pinned by `wizard-power-user-managed-install.test.ts`. **External-link hardening** (change: harden-external-link-handling, #13): `createMainWindow` registers `webContents.setWindowOpenHandler` + `will-navigate` BEFORE `loadURL` so `target="_blank"` / `window.open` + bare-`<a>` external nav route through `shell.openExternal` (system browser) instead of replacing dashboard. `will-navigate` **OAuth-aware** via `decideWillNavigate(serverUrl, webContents.getURL(), url)` (change: fix-oauth-blocked-by-external-link-guard) — trap-guard fires only when leaving dashboard, so mid-login on OAuth provider (Google/GitHub/generic OIDC) provider-internal multi-step nav allowed + callback redirect lands cleanly. `setWindowOpenHandler` unchanged. See change: electron-server-launch-controls — loadFile(loading.html) replaces inline data:text/html. mainWindow attaches preload exposing piDashboard. Registers ipcMain handlers `dashboard:request-launch`, `dashboard:open-doctor`, `dashboard:read-server-log`. Forwards `dashboard:launch-status` push events. Tray hooks pass `requestServerLaunch` + `isManagedServerRunning`. |
| `packages/electron/src/lib/link-handling.ts` | Pure helpers used by Electron shell. **`isSameOriginUrl(href, serverOrigin)`** — classifies URLs as same-origin vs external (handles absolute, relative, fragment-only, malformed inputs; see change: harden-external-link-handling, #13). **`decideWillNavigate(serverOrigin, currentUrl, targetUrl) → "allow" | "open-external" | "cancel"`** — OAuth-aware decision wrapper for `will-navigate` callback: while BrowserWindow shows non-dashboard origin (mid-OAuth) returns `"allow"` so provider login flows proceed; on dashboard composes `isSameOriginUrl` to decide between `"allow"` (same-origin) + `"open-external"` (external trap-guard); fail-closes to `"cancel"` on unparseable `serverOrigin` (see change: fix-oauth-blocked-by-external-link-guard). No Electron imports; 23 unit tests in `packages/electron/src/__tests__/link-handling.test.ts`. |
| `packages/electron/src/lib/server-error-classification.ts` | Pure `isDeadlineOrChildExitError(message)` returns true for messages starting with "Server did not respond within" or "Server child process exited prematurely". Drives main.ts routing: terminal errors → Setup/Retry/Quit dialog; deadline/child-exit → fall through to loading page. Drift-guarded against `buildServerStartupError` prefixes. See change: tighten-electron-server-startup-deadline. |
| `packages/electron/src/lib/launch-source.ts` | `spawnFromSource` accepts `onExitAfterReady` callback; forwards to `launchDashboardServer`. See change: streamline-electron-bootstrap-and-recovery (Failure 5). |
| `packages/electron/src/lib/server-lifecycle.ts` | Health check → tsx binary spawn (inlined config/health, no shared pkg imports). **Group-16 Failures 3+5** (change: streamline-electron-bootstrap-and-recovery): exports `makeServerWatchdog`, `setGracefulShutdownInProgress`, `isGracefulShutdownInProgress`. `readServerLogTail` reads from `getDashboardServerLogPath()` (was hard-coded installer log path). Watchdog factory consumes `LaunchOpts.onExitAfterReady` from `server-launcher.ts`. **System-Node-when-safe** (change: prefer-system-node-when-safe): `pickNodeRuntime()` wires `pickNodeForServer` against live runtime (caches `node --version` probe to avoid spawning twice per startup). `resolveTsxCommand` Windows arm + `launchServer` PATH/runtime resolution both call it; logs `[server-lifecycle] Using system Node vX.Y.Z at <path>` or `Using bundled Node at <path>`. **15s startup deadline + cause-aware error wording** (change: tighten-electron-server-startup-deadline, was 60s in fix-electron-windows-installer-and-server-bootstrap/Defect 4): exports `SERVER_READY_DEADLINE_MS = 15_000` + pure helper `buildServerStartupError(...)` rendering "Server child process exited prematurely (...)" when `ready.error` mentions exit, vs. "Server did not respond within 15 seconds (...)" when deadline elapsed. Pre-fix message conflated both cases, misleading when child died in <1s. `launchViaCli` + `launchServer` use same constant + helper. See change: electron-server-launch-controls — Adds `requestServerLaunch({force?})` (idempotent, shared inflight Promise, failure-as-value), `isManagedServerRunning()`, `readServerLogTail(lines)`, `onLaunchStatus(cb)` emitter. `force:true` shuts down via /api/shutdown then spawns. Tests in `__tests__/server-lifecycle-launch.test.ts`. Deadline tightened back to 15s because beyond that point failure is almost always terminal; loading.html (resources/loading.html) is better surface than frozen splash. |
| `packages/electron/src/lib/doctor.ts` | Electron-only doctor checks (Electron version, bundled Node, bundled npm, server-code path, offline-packages bundle, server-launch sanity test). Delegates portable checks to `packages/shared/src/doctor-core.ts:runSharedChecks`. Post-stamps `section` + `suggestion` from shared maps. Routes spawns through `safeExec` (`timeoutMs: 15000` for cold-start probes). Wraps mandatory ops in `assumedMandatory`. Top-level `try/catch` returns fallback report on uncaught throw. |
| `packages/electron/src/lib/doctor-window.ts` | `openDoctorWindow()` factory. BrowserWindow 1000×720. Single-instance focus-reuse. `doctor:open-log` opens `~/.pi/dashboard/server.log` via `getDashboardServerLogPath()` (was wrong installer log; see change: streamline-electron-bootstrap-and-recovery Failure 3). Registers IPC handlers `doctor:run` (concurrency-serialized), `doctor:open-log`, `doctor:open-doctor-log` (returns `{exists:false}` when absent), `doctor:run-setup`, `doctor:copy`, `doctor:open-managed-dir`. Wires `closed` → null reference. |
| `packages/electron/src/lib/doctor-bridge-contract.ts` | `DoctorBridge` TypeScript interface. Imported by preload + renderer. Channel-name-drift breaks type-check. |
| `packages/electron/src/preload/doctor-preload.ts` | Implements `DoctorBridge` via `contextBridge.exposeInMainWorld("electron", { doctor: … })`. |
| `packages/electron/src/renderer/doctor.html` | Hand-rolled HTML+CSS. Reuses `wizard.html` tokens. One `<table>` per section. Toolbar `[Re-run]` `[Copy as Markdown]` `[Copy as Plain]` `[Open server log]` `[Open doctor log]` `[Run setup wizard]`. `[Re-run]` disabled while in flight. Fallback row on IPC rejection. **Force-reinstall danger card** (change: streamline-electron-bootstrap-and-recovery): always-visible bottom section with `[Force reinstall…]` button invoking `window.piDashboard.forceReinstall()`; wipes Electron-owned packages + bundled-node + offline-cache, then reinstalls. Caller-side confirm before IPC call. |
| `packages/electron/src/renderer/wizard.html` | Hand-rolled HTML+CSS first-run setup wizard. Slimmed 883→402 lines (change: streamline-electron-bootstrap-and-recovery): 4 steps `step-welcome` → `step-select-packages` (required disabled + bundled extensions toggleable, populated from `wizard:get-catalog` returning assembled three-tier `InstallableList` minus npm-registry tier) → `step-progress` (streams `installStandalone()` callbacks) → `step-done` (deep-links `/settings?tab=provider-auth`). Deleted steps: `step-mode`, `step-bridge-install`, `step-install`, `step-apikey`, `step-recommended`, `step-v2-status`, `step-v2-packages`, `step-v2-error`. `wizard:save-selection` persists toggles to `~/.pi/dashboard/installable.json`. |
| `packages/electron/src/lib/app-menu.ts` | App menu with About dialog + Doctor on all platforms. Doctor menu entry opens `openDoctorWindow()` (replaces native `dialog.showMessageBox`). |
| `packages/electron/src/lib/tray.ts` | System tray with platform-specific icons (template on macOS, ico/png on Win/Linux). See change: electron-server-launch-controls — Dynamic menu item `Start server` / `Restart server` driven by 3s `isManagedServerRunning` poll. Pure `buildTrayMenuTemplate({isRunning, onLaunch, onShow, onQuit})` exported for tests. |
| `packages/electron/src/preload.ts` | Exposes `wizardApi` (existing) + new `piDashboard` namespace: `requestLaunch`, `openDoctor`, `readServerLog`, `onStatus`. Same preload attached to wizard window + main window; each renderer uses what it needs. See change: electron-server-launch-controls. |
| `packages/electron/src/lib/dependency-installer.ts` | Async npm install of pi, openspec, tsx into `~/.pi-dashboard/` via bundled Node. **`installStandalone(onProgress?, skipPackages?)`** (change: streamline-electron-bootstrap-and-recovery): second arg is `string[]` of package names to bypass; preflight passes `diff.upToDate` so per-launch reconcile skips current packages, reinstalling only stale/missing/corrupt. Default empty set = full install (preserves legacy callers). **Cross-platform contract** (changes: ensure-windows-system-path, fix-windows-npm-spawn-fallback, prevent-git-credential-manager-hang, offline-install-per-package-progress): `buildInstallEnv()` calls `ensureWindowsSystemPath(env)` (no-op on POSIX); `runNpmInstall` + `runNpmWithArgv` route spawn through `buildSafeArgv()` — Windows wraps extensionless `npm` via `cmd.exe /d /s /c`, POSIX passthrough (still `shell:false`). `runOfflineInstall` fans per-package progress under wizard's known step names (`pi-coding-agent`, `openspec`, `tsx`) so UI icons move during offline installs. **`installRecommendedExtensions` GCM-bypass env vars (`GIT_TERMINAL_PROMPT=0`, `GCM_INTERACTIVE=Never`, `GIT_ASKPASS=echo`, `SSH_ASKPASS=echo`) gated on `process.platform === "win32"`** — macOS/Linux Electron inherits working credential helper / ssh-agent (Keychain, libsecret, gnome-keyring); forcing `GIT_ASKPASS=echo` there would break private-repo clones with confusing error. GCM hang is Windows-specific. All four still guarded with `??=` so user-set values preserved. |
| `packages/electron/src/lib/dependency-detector.ts` | Detects pi, openspec, Node.js on system PATH + managed install. **AppImage self-recursion guard** (change: fix-electron-appimage-cli-self-detection): `detectPiDashboardCli` rejects candidates matching `isAppImageSelfHit(path)` (plus existing `_npx` filter) so power-user mode falls through to standalone tsx + `cli.ts` when only `pi-dashboard` on PATH is AppImage's own launcher (`packagerConfig.executableName: "pi-dashboard"` collides by design). `detectPi` + `detectSystemNode` apply same guard symmetrically on registry-resolved path — belt-and-braces beyond `whereStrategy` filter. **Windows extension filter** (change: fix-electron-windows-installer-and-server-bootstrap, Defect 3): pure helper `pickSpawnableShim(rawWhereOutput, platform)` on `win32` prefers candidates ending `.cmd`/`.exe`/`.bat`/`.ps1` over extensionless POSIX shim from npm-global. `spawn()` without `shell:true` cannot invoke extensionless shim on Windows, so pre-fix `lines[0]` produced `ENOENT`. POSIX (single-line `which`) unchanged. Locked by `dependency-detector-windows-extensions.test.ts`. |
| `packages/electron/src/lib/bundled-node.ts` | Resolves bundled Node.js/npm paths in Electron resources |
| `packages/electron/src/lib/wizard-window.ts` | First-run setup wizard window with preload bridge. Default 820×780, `minWidth: 640`, `minHeight: 560`, `resizable: true` so recommended-extensions step's button row never clipped. See change: enlarge-wizard-window. |
| `packages/electron/src/lib/ensure-windows-path.ts` | `ensureWindowsSystemPath(env = process.env)` — prepends `%SystemRoot%\System32`, `%SystemRoot%`, `%SystemRoot%\System32\Wbem`, `%SystemRoot%\System32\WindowsPowerShell\v1.0`, `%APPDATA%\npm`, + any of 5 standard Git-for-Windows install dirs containing `git.exe`; pins `env.ComSpec` to verified `System32\cmd.exe`. **First line is `if (process.platform !== "win32") return;`** — strict no-op on POSIX. Idempotent (case-insensitive dedupe). Called at three sites: `main.ts` startup, `dependency-installer.ts::buildInstallEnv`, `installRecommendedExtensions` (right before pi's `DefaultPackageManager` loads). See change: ensure-windows-system-path. |
| `packages/electron/src/lib/force-reinstall.ts` | Surgical reset of Electron-owned packages preserving everything else under `~/.pi-dashboard/` + everything outside. `planSafeWipe(managedDir)` enumerates `node_modules/`, classifies each entry against `ELECTRON_OWNED_PACKAGES` from `@blackbelt-technology/pi-dashboard-shared/managed-package-whitelist.js` — owned go to `wipe[]`, user-installed go to `preserve[]`. Always-wipe: `<managedDir>/node/` + `<managedDir>/.offline-cache/`. `forceReinstall({managedDir, bundledNodeDir, installStandalone, onProgress})` resolves each wipe path against `managedDirAbs` + refuses any escapee (belt-and-braces); wipes via `rmSync({recursive:true, force:true})`, restores managed Node via `installManagedNode`, then invokes injected `installStandalone`. Returns `{ok, wiped[], preserved[], error?}`. Pure `formatPlanSummary(plan)` for audit panels. Consumed by Doctor `[Force reinstall…]` + loading.html `[Force reinstall]`. See change: streamline-electron-bootstrap-and-recovery. |
| `packages/electron/src/lib/installable-catalog.ts` | Pure-I/O assembler producing `InstallableList` (`schemaVersion: 2`) from the two bundled sources shipped with the Electron app. `readCoreFromOfflinePackagesJson(resourcesPath)` reads `<resourcesPath>/offline-packages/manifest.json` (falls back to `pins`/`packages` shape) emitting `{name, version, required:true, kind:"npm", source:"offline-cache"}` rows. `readBundledExtensionsFromGitCache(resourcesPath)` walks `<resourcesPath>/bundled-extensions/<id>/package.json` emitting `{name, version, required:false, kind:"pi-extension", source:"bundled-git", displayName?}`; cross-references `RECOMMENDED_EXTENSIONS` for displayName. Two-tier only (`core` + `extension`); `npm-registry` discovery deferred to post-install Settings. Missing resource dirs return `[]` (dev/opt-out builds boot). `assembleCatalog({resourcesPath})` concatenates both tiers. See change: streamline-electron-bootstrap-and-recovery (Group 9). |
| `packages/electron/src/lib/launch-source.ts → extractedSourceIsHealthy` | Pure helper. Returns true iff cliPath exists AND `resolveJitiFromAnchor(cliPath)` non-null. Gates extract+install re-run when version marker matches but tree degraded. See change: fix-electron-extracted-jiti-and-stdio-capture. |
| `packages/electron/src/lib/preflight-reconcile.ts` | Pure-I/O launch-time reconciliation between installed managed packages + offline-cache pin floor. `readManagedInventory(managedDir)` reads `<managedDir>/node_modules/<pkg>/package.json` for every `ELECTRON_OWNED_PACKAGES` entry, returns `Map<pkg, version|null>`. `readOfflinePackagePins({resourcesPath?, buildTimePinsPath?})` reads runtime `manifest.json` then dev-build `offline-packages.json` fallback. `compareWithPins(inventory, pins, corruptHint?)` classifies each entry `current`/`stale`/`missing`/`corrupt`, returns `InventoryDiff {diffs, needsAction, upToDate, missing, stale, corrupt}`. `runPreflight(opts)` orchestrates all three + disambiguates missing-vs-corrupt via `detectExistingPackageJsons`. `formatDiagnosis(diff)` renders single-line UI text. `compareRunningServerVersion(running, app)` returns `"match"`/`"running-newer"`/`"running-older"`/`"unknown"` for cross-version banner. No spawn, no network; perf target <500ms cold, <100ms warm. Consumed by main.ts startup + loading.html recovery + Doctor diagnostics. See change: streamline-electron-bootstrap-and-recovery. |
| `packages/electron/src/lib/legacy-cleanup.ts` | `cleanupLegacyStateFiles(managedDir)` — idempotent best-effort `rmSync` of stray `mode.json` (replaced by fs presence check in `power-user-install.ts::isManagedDirPopulated`). Per-path errors logged via `log()`. Wired into V2 launch path in `main.ts` immediately before `selectLaunchSource`. Legacy `LAUNCH_SOURCE_V2=false` path untouched (still reads `mode.json` via `wizard-state.ts::isFirstRun`). Tested in `__tests__/legacy-cleanup.test.ts`. See change: streamline-electron-bootstrap-and-recovery. |
| `packages/electron/src/lib/power-user-install.ts` | Every-launch startup gate (no longer first-run-only). `isManagedDirPopulated(managedDir)` — `fs.existsSync` for any `ELECTRON_OWNED_PACKAGES` entry under `<managedDir>/node_modules/` (replaces `mode.json`-backed `isFirstRun`). Delegates to shared `isPackageInstalledOnDisk` (was direct `existsSync` + JSON parse). See change: fix-is-npm-package-installed-exports-map. `decideStartupAction({piFound, bridgeFound, managedPopulated, preflightNeedsAction})` → `{kind: "skip"|"preflight-install"|"wizard"}`. Pure helper. Pinned by `wizard-trigger-and-startup-action.test.ts`. See change: streamline-electron-bootstrap-and-recovery (replaces fix-electron-windows-installer-and-server-bootstrap Defect 1 first-run gate). |
| `packages/electron/src/lib/recovery-ipc.ts` | `registerRecoveryIpc({installStandalone})` — three loading-page handlers: `dashboard:check-inventory` runs `runPreflight` + `formatDiagnosis`; `dashboard:reinstall-managed` runs `installStandalone(progress, skipPackages=diff.upToDate)` with inflight-Promise coalescing; `dashboard:force-reinstall` shows confirm dialog (cancel default) then `forceReinstall(...)`. Progress events fan to every BrowserWindow via `dashboard:install-progress` + `dashboard:launch-status`. Wired in `main.ts` startup beside `registerPiDashboardIpc()`. See change: streamline-electron-bootstrap-and-recovery. |
| `packages/electron/src/lib/wizard-state.ts` | Slimmed (change: streamline-electron-bootstrap-and-recovery): removed `isFirstRun`, `readModeFile`, `writeModeFile`, `ModeConfig`. Replaced by `isManagedDirPopulated` in `power-user-install.ts` (fs presence check, no state file). Retains helper invoking `legacy-cleanup.ts::cleanupLegacyStateFiles`. All callers updated. |
| `packages/electron/src/lib/pick-node.ts` | `pickNodeForServer(PickNodeInput): PickNodeResult` — discriminated union `{kind:"bundled"\|"system"\|"execpath-fallback"}`. Priority: bundled `Resources/node/bin/node` (POSIX) / `Resources/node/node.exe` (Win) → system Node → `process.execPath` fallback (caller sets `ELECTRON_RUN_AS_NODE=1`). All I/O injected via `existsSync` for pure testing. `bundledNodeDirFromResources(resourcesPath)` also exported. 8 unit tests in `packages/electron/src/lib/__tests__/pick-node.test.ts`. See change: fix-electron-server-launch-node-bin. |
| `packages/electron/forge.config.ts` | Electron Forge config: DMG, DEB, AppImage, NSIS makers, icon, extraResources. **NSIS naming overrides** (change: fix-electron-windows-installer-and-server-bootstrap): NSIS maker's `getAppBuilderConfig` pins `productName`, `appId`, `nsis.artifactName`, `nsis.shortcutName`, `nsis.uninstallDisplayName` all to `pi-dashboard`. Without override, electron-builder's NSIS install-dir fallback reads npm `name` slash-stripped + produces `@blackbelt-technologypi-dashboard-electron`. Locked by `forge-config-naming.test.ts`. **macOS deployment-target floor** (change: add-darwin-x64-build, 6b): `packagerConfig.extendInfo.LSMinimumSystemVersion = "10.15"` pins minimum macOS version in `Info.plist`. Pairs with `MACOSX_DEPLOYMENT_TARGET=10.15` from `Make Electron distributables` step in `publish.yml` (every Mach-O declares 10.15 as `LC_BUILD_VERSION.minos`) + CI verification step that mounts DMG + asserts both match — runner-image upgrade or source-built native module cannot silently raise floor without failing job. **Arch-tagged DMG basename** (change: fix-darwin-dmg-arch-collision): `@electron-forge/maker-dmg` `name` field composed at config-eval as `` `PI-Dashboard-darwin-${process.arch}-${pkgVersion}` `` (`pkgVersion` from `packages/electron/package.json` at module top). Both macOS matrix legs (`macos-14`/arm64, `macos-15-intel`/x64) previously emitted static `PI Dashboard.dmg`, causing `softprops/action-gh-release@v2` to overwrite one arch with other on upload (dedups by basename). Arch-tagged also matches deb (`_amd64`/`_arm64`) + Windows portable (`-arm64`/`-x64`) conventions. `title` field stays `"PI Dashboard"` so volume title remains friendly. Locked by `forge-config-dmg-naming.test.ts` (6 tests: both arches, version interpolation, title preservation, exact pattern). |
| `packages/electron/.bundle-stamp` | Gitignored JSON sentinel `{builtAt, srcMtime, bundlerMtime}`. Authoritative reference for bundle-server cache gate. Written by `build-installer.sh` post-bundle. See change: fix-build-installer-stale-server-bundle. |
| `packages/electron/scripts/_bundle-stamp.mjs` | Node helper. Subcommands `check` / `write` / `age`. Backs `is_bundle_stale` / `write_bundle_stamp` / `format_stamp_age` bash wrappers in `build-installer.sh`. Tracked source roots: `packages/{server,shared,client,extension,dashboard-plugin-runtime}/src`. Tracked extensions: `.ts .tsx .js .mjs .cjs .json .html .css .svg`. Stale reasons: `stamp-missing` / `source-newer:<relpath>` / `bundler-newer`. Env: `BUNDLE_STAMP_PATH`, `BUNDLE_SRC_ROOTS`, `BUNDLER_SCRIPT`, `PROJECT_DIR`. See change: fix-build-installer-stale-server-bundle. |
| `packages/electron/scripts/build-installer.sh` | Build script: native + Docker cross-platform (`--linux`, `--windows`, `--all`). Writes `packages/electron/.bundle-stamp` after every successful bundle. Re-runs `bundle-server.mjs` when `source-newer` / `bundler-newer` / `stamp-missing`. Cache hit logs `✓ Bundled server cache hit (built <age>, stamp matches)`; stale logs `↻ Bundled server stale (reason=<token> [file=<path>]) — re-bundling`. Escape hatch `npm run clean:resources`. See change: fix-build-installer-stale-server-bundle. **`--mac-both`** (change: add-darwin-x64-build) builds BOTH macOS DMGs (arm64 + x64) on Apple Silicon in one invocation: 2 sequential `build_native_one_arch` calls with sentinel-driven cache invalidation between them (`resources/.last-arch`). Refuses on Intel hosts (Rosetta one-way, x64→arm64 cross-compile not supported locally) + on non-darwin hosts. Cross-arch x64 builds on Apple Silicon auto-detect Rosetta 2 via `arch -x86_64 /usr/bin/true` (fail-fast with `softwareupdate --install-rosetta --agree-to-license` hint if missing) + wrap `bundle-server.mjs` in `arch -x86_64` + set `TARGET_ARCH=x64` so node-pty's prebuilt x64 binary downloaded. `--mac-both` post-build smoke summary mounts each DMG + reports inner Mach-O arch tag (`arm64` / `x86_64`) via `file` so silent arch-mismatch can't ship. Arch-aware cache wipe also fires on single-arch back-to-back invocations (`--arch arm64` then `--arch x64`) — no manual cleanup needed. Pure helpers: `maybe_wipe_arch_caches`, `verify_rosetta_or_fail`, `build_native_one_arch`. **Stale-pin invalidation** (change: streamline-electron-bootstrap-and-recovery): `maybe_wipe_arch_caches` mtime-compares `offline-packages.json` vs cached `resources/offline-packages/manifest.json`; newer pins file wipes cache so bundle step re-runs with current pins. Prevents shipping installer that pins one version while bundled cacache holds another. |
| `packages/electron/scripts/build-local.sh` | Convenience wrapper around `build-installer.sh` defaulting `BUNDLE_OFFLINE_PACKAGES=1` + `BUNDLE_RECOMMENDED_EXTENSIONS=1` so single command produces fully offline-capable host installer (`out/make/<platform>/...`). Preserves user-supplied env values via `${VAR:-1}`. Delegates to canonical builder; all cache build + invalidation logic stays in `build-installer.sh`. See change: streamline-electron-bootstrap-and-recovery (group 10). |
| `packages/electron/scripts/docker-make.sh` | Docker entrypoint: platform-aware native module handling, ZIP for Windows |
| `packages/electron/scripts/Dockerfile.build` | Docker image for cross-platform builds (node:22-bookworm-slim + build tools) |
| `packages/electron/scripts/bundle-server.mjs` | Bundles dashboard server source + deps into `resources/server/` (`--source-only` for cross-builds). Node-native ESM script (replaces `bundle-server.sh`) so Windows electron CI runs without MSYS/bash. Uses `fs.cpSync` / `fs.chmodSync` / recursive `readdir` instead of `cp -R`/`find`/`chmod`/`du`. Verified bit-parity with old shell script: identical 2251-file layout. See change: eliminate-bash-on-windows-runners. **Architectural lock**: synthetic `package.json` deliberately does NOT declare `@earendil-works/pi-coding-agent` / legacy `@mariozechner/pi-coding-agent` (or any managed-dir-resident dep). Bundled tree only contains workspace deps (`fastify`, `ws`, `node-pty`, etc.) directly imported by bundled `cli.ts`. pi/openspec/tsx live in `~/.pi-dashboard/`, installed by `installStandalone()` from offline cacache (`offline-packages.json`). Earlier `/opsx:apply` against `fix-electron-windows-installer-and-server-bootstrap` proposed adding `dependencies: { "@mariozechner/pi-coding-agent": "0.70.0" }` here — reverted as architecturally wrong (would duplicate ~10MB + create version-drift vs. offline cacache). Bundle stays ~80MB; if it climbs to ~160MB that's regression marker. Single source of truth `BUNDLE_WORKSPACE_PKGS = ["server", "shared", "extension", "dashboard-plugin-runtime"]` controls which workspaces get copied + listed in synthetic `package.json` `workspaces` + stripped of `__tests__`. After `npm install`, dereferences symlinks under `node_modules/@blackbelt-technology/` (npm workspaces creates them; Linux `zip -r` preserves; Windows extractors don't — they become zero-byte garbage → `MODULE_NOT_FOUND` on Windows). MUST stay in sync with `packages/server/package.json#dependencies` whenever a workspace package is added (drift cause of `npm error notarget` builds; see `docs/troubleshooting-windows-installer.md` §4.2–4.3). See change: fix-electron-windows-installer-and-server-bootstrap (D5 reconsidered). |
| `packages/electron/offline-packages.json` | Pinned versions of pi-coding-agent / openspec / tsx that get bundled as offline npm cacache per release (see change: `electron-offline-bundled-packages`). Parity-pinned vs `ELECTRON_OWNED_PACKAGES` in `packages/shared/src/managed-package-whitelist.ts` by `managed-package-whitelist-parity.test.ts`. |
| `packages/electron/package.json` | Workspace package manifest. Scripts added (change: streamline-electron-bootstrap-and-recovery): `build:local` → `bash scripts/build-local.sh` (defaults `BUNDLE_OFFLINE_PACKAGES=1` + `BUNDLE_RECOMMENDED_EXTENSIONS=1`); `build:local:offline` forces both regardless of env; `clean:resources` wipes `resources/{server,node,offline-packages,bundled-extensions}`. |
| `packages/electron/scripts/bundle-offline-packages.sh` | Build-time script that runs `npm install --os=<os> --cpu=<cpu> --ignore-scripts` for pinned versions, tars resulting `_cacache/` (pax format — ustar too narrow), writes `manifest.json` with SHA-256, enforces 100 MB hard budget. Opt-in via `BUNDLE_OFFLINE_PACKAGES=1`. |
| `packages/electron/resources/loading.html` | Loading-page HTML resource. Polls `/api/health`, shows error UI after ~15s, exposes Start-server button + Open-Doctor link + collapsible server-log panel. Reads `serverUrl` + base64 `knownServers` from query string. Uses `window.piDashboard` IPC bridge. See change: electron-server-launch-controls. **Recovery flow** (change: streamline-electron-bootstrap-and-recovery): on connection-failure timeout, calls `piDashboard.preflightCheck()`; renders single-line diagnosis row (`formatDiagnosis` text); exposes `[Reinstall]` (runs `piDashboard.reinstall()`) + `[Force reinstall (wipe + reinstall)]` (runs `piDashboard.forceReinstall()` with confirm). Force button hidden when IPC unavailable. Buttons mutually disable during in-flight work. |
| `packages/electron/resources/offline-packages/manifest.json` | Offline-cache manifest (`{bundledAt, targetPlatform, tarball, tarballBytes, sha256, packages}`). Consumed at runtime by `dependency-installer.ts` via `resolveOfflinePackages()`. |
| `packages/electron/resources/offline-packages/npm-cache.tar.gz` | gzipped npm cacache used by first-run offline install. Extracted to `~/.pi-dashboard/.offline-cache/`, verified by SHA-256, consumed by `npm install --offline`, then deleted to reclaim ~140 MB. |
| `packages/electron/src/lib/offline-packages.ts` | Pure helpers: `parseOfflineManifest`, `resolveOfflinePackages`, `fileSha256`, `extractOfflineCache`, `buildOfflineInstallArgs`, `selectInstallStrategy`. 19 unit tests in `packages/electron/src/__tests__/offline-packages.test.ts`. |
| `packages/electron/scripts/bundle-recommended-extensions.mjs` | Opt-in (via `BUNDLE_RECOMMENDED_EXTENSIONS=1`) build-time bundler that clones each id in `BUNDLED_EXTENSION_IDS` into `packages/electron/resources/bundled-extensions/<id>/`, records `.bundled-sha`, enforces SPDX allowlist (MIT, Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC) + 15 MB total size budget. Clones from `entry.bundleSource ?? entry.source` so npm-sourced entries still bundle via git URL. See change: bundle-first-party-extensions, npm-publish-first-party-extensions. |
| `packages/electron/src/lib/dependency-installer.ts` → `installBundledExtensions` | First-run activation of pre-bundled extensions. Copies `<resourcesPath>/bundled-extensions/<id>/` into pi's git cache (`~/.pi/agent/git/<host>/<path>/`), runs `npm install --omit=dev` if runtime deps declared, then calls `manager.addSourceToSettings(gitUrl)` + `settingsManager.flush()`. Uses `effectiveSource = entry.bundleSource ?? entry.source` for `parseBundledGitSource` + `addSourceToSettings` so npm-sourced runtime entries activate from bundled git tree. Returns activated ids so `installRecommendedExtensions` can skip them with `output: "Already installed (bundled)"`. See change: npm-publish-first-party-extensions. |
| `packages/electron/src/lib/wizard-badge.ts` | Pure `classifyProgressBadge(output)` helper (`bundled` / `system` / null) shared between wizard HTML inline JS + unit tests. |
| `packages/electron/scripts/docker-make.sh` | Docker entrypoint: bundles server, installs native deps, runs Forge make |
| `packages/electron/scripts/Dockerfile.build` | Docker image for cross-platform builds (node:22-bookworm-slim + build tools) |
| `packages/electron/scripts/test-server-launch.sh` | Docker-based test for server launch on clean Linux |
| `packages/electron/scripts/test-electron-install.sh` | Full e2e Docker test: install, wizard, server launch, health check |
| `packages/electron/scripts/test-electron-install-inner.sh` | Inner test script run inside Docker container |
| `packages/electron/resources/icon.png` | Master 1024×1024 app icon (π on dark navy) |
