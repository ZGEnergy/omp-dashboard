# sample-jj

A throwaway jujutsu (jj) fixture for exercising the pi-dashboard VCS panel.

Initialized as a real jj repo at container start (see `docker/test-entrypoint.sh`).
Pin it as a workspace from the dashboard UI to view the jj log and working copy.
