# sample-git

A throwaway git fixture for exercising the pi-dashboard VCS panel.

Initialized as a real git repo at container start (see `docker/test-entrypoint.sh`).
Pin it as a workspace from the dashboard UI to view status, log, and diffs.
