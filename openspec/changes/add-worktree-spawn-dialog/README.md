# add-worktree-spawn-dialog

Add +Worktree Session button to directory groups: unified dialog spawns existing worktrees or creates a new one under .worktrees/<slug>, then spawns a pi session in it. Group worktree sessions under parent repo (mirrors jjState.workspaceRoot precedent).
