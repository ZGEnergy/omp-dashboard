## 1. Move button to header

- [ ] 1.1 Move the "+ New Change" button from the expanded content area to the header row `div` (next to "Bulk Archive" and refresh)
- [ ] 1.2 Style it consistently with "Bulk Archive" — bordered button, `text-[10px]`, green/blue hover accent
- [ ] 1.3 Remove the old "+ New Change" button from the bottom of the expanded content

## 2. Tests

- [ ] 2.1 Update existing tests in `OpenSpecSection.test.tsx` to reflect new button placement (visible even when collapsed)
