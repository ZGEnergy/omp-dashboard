RESULT: SUCCESS

Implemented pi-version cache reconnect fix.

- Moved last emitted pi version from module-global state to BridgeContext/bridge-instance state.
- Reset that cache on websocket reconnect and new/fork/resume session attach.
- Preserved failed-read warning rate limiting and added explicit coverage.
- Added tests for reconnect re-emission, second-session emission, unchanged-version suppression, version changes, undefined reads, and warning rate limiting.

Verification:
- `HOME=$(mktemp -d) node_modules/.bin/vitest --root packages/extension --config packages/extension/vitest.config.ts run src/__tests__/pi-version-tracker.test.ts`: 14 passed.
- Related extension tests (`pi-version-tracker`, `session-sync`, `git-poll`): 31 passed.
- Biome check on changed TypeScript files: passed.
- Extension TypeScript check remains blocked by pre-existing errors in ask-tool/ask-tool tests and project-init-dox-kb-config; no errors point to this change.
- Repository has no `.pre-commit-config.yaml`, so pre-commit could not run.
