# split-state.ts — index

Per-session split state (`open`,`ratio`,`orientation`), localStorage key `pi-dashboard:split:<id>`. `clampRatio` [0.25,0.75]. `useSplitState` hook. Best-effort read/write. See change: split-editor-workspace.
